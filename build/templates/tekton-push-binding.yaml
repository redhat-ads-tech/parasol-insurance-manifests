apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata:
  name: parasol-insurance-push-binding
  namespace: {{ .Values.namespace }}
spec:
  params:
    - name: git-revision
      value: $(body.checkout_sha)
